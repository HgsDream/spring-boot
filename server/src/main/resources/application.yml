server:
  port: 8761
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaault-zone: http://${eureka.instance.hostname}:${server.port}/eureka/